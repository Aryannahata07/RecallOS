# Project Rules for RecallOS

## File Size & Component Modularization Rule
- No single file should exceed 300 lines of code.
- Always break down UI features and pages into modular, focused components.
- Move reusable components, independent tab views, modals, and helper utilities into separate files under `src/components/`, `src/types/`, and `src/lib/`.
